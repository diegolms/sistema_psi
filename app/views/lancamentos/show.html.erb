<% if action_name.to_sym == :show %>
       <div class="breadcrumbs">
            <div class="col-sm-4">
                <div class="page-header float-left">
                    <div class="page-title">
                        
                    </div>
                </div>
            </div>
            <div class="col-sm-8">
                <div class="page-header float-right">
                    <div class="page-title">
                        <ol class="breadcrumb text-right">
                          <%= link_to 'Back', root_path, class: "btn btn-outline-secondary mx-2" %>
                          <%= link_to 'DOWNLOAD PDF', lancamento_path(::Lancamento.find(params[:id]), format: :pdf), class: "btn btn-outline-success my-2 my-sm-0" %>
                        </ol>
                    </div>
                </div>
            </div>
        </div>
<% end %>

<div class="invoice-box">
		<object
		  data='<%=File.join(Rails.root, "tmp/relatorio.pdf")%>'
		  type="application/pdf"
		  width="100%"
		  height="100%">
		  <iframe
			src='<%=File.join(Rails.root, "tmp/relatorio.pdf")%>'
			width="100%"
			height="100%"
			style="border: none;">
			<p>Your browser does not support PDFs.
			  <a href="https://example.com/test.pdf">Download the PDF</a>.</p>
		  </iframe>
		</object>

    </div>